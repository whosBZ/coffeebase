version: "3.8"

services:
  #1 Database Layer
  postgres-db:
    image: postgres:16-alpine
    container_name: coffeebase_postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: coffeebase_prod
    volumes:
      - postgres_data:/var/lib/postgres/data
